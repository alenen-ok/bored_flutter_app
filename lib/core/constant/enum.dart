enum Destination { Randomizer, Search, Favourites, Settings }

enum ActivityType { recreational, cooking, education, relaxation,
  social, charity, music, busywork, diy}

enum CostType { free, cheap, medium, expensive}

enum GroupType { solo, small_company, big_company}

enum AccessibilityType { novice, junior, middle, senior}

enum ErrorType { something_went_wrong, no_internet_connection}
